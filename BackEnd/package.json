{
  "name": "grimorio-digital-back",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx knex --knexfile knexfile.js --env development migrate:latest && nodemon src/server.js",
    "start": "npx knex --knexfile knexfile.js --env production migrate:latest && node src/server.js --prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "crypto": "^1.0.1",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "knex": "^3.1.0",
    "nodemon": "^3.1.14",
    "pg": "^8.20.0",
    "sqlite3": "^6.0.1"
  }
}
